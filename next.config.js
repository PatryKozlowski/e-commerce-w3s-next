/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['www.w3schools.com', 'firebasestorage.googleapis.com']
  }
}

module.exports = nextConfig
